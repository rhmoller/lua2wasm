build/
build-em/
# Created when ctest is accidentally run from the repo root.
Testing/
*.wasm
*.wat
# The embedded runtime prelude is checked-in source, not a build artifact.
!runtime/prelude.wat
.botface/
.playwright-mcp/
notes/
official-tests/
